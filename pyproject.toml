[project]
name = "cf-yx-dns"
version = "0.1.0"
description = "Cloudflare DNS 优选 IP 自动更新dns工具"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cloudflare>=5.1.0",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
